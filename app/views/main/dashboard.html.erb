<h1>Dashboard</h1>

<p>Hello, <%= current_user.username %></p>

<%= link_to "Log Out", destroy_user_session_path, method: 'delete' %>

<%= button_to 'Backing Tracks', backing_tracks_path, method: 'get' %>
<%= button_to 'Recordings', recordings_path, method: 'get' %>
<%= button_to 'Mixes', mixes_path, method: 'get' %>

<p>Here are your recordings.</p>

<p>Here are your mixes.</p>
<ul>
    <li>Birlinn - Mixed - Jane, Adam, Irena</li>
</ul>